[project]
name = "axezent-opac016-type-c-exact-kappa"
version = "1.0.0"
description = "Standalone proof and reproducibility package for AXZ-OPAC-016."
requires-python = ">=3.11"
authors = [{name = "Shawn Calvin Snelling"}]
license = {text = "MIT"}

[tool.pytest.ini_options]
testpaths = ["tests"]
